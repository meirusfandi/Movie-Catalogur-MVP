package com.example.root.moviecatalogue.views.upcoming;import android.net.Uri;import android.os.Bundle;import android.support.annotation.NonNull;import android.support.annotation.Nullable;import android.support.v4.app.Fragment;import android.support.v4.widget.SwipeRefreshLayout;import android.view.LayoutInflater;import android.view.View;import android.view.ViewGroup;import com.example.root.moviecatalogue.R;import com.example.root.moviecatalogue.model.MovieModel;import com.example.root.moviecatalogue.views.navigasi.MainFragment;import java.util.List;public class UpcomingFragment extends MainFragment implements SwipeRefreshLayout.OnRefreshListener{    public UpcomingFragment() {    }    @Override    public void onViewCreated(@NonNull View view, @Nullable Bundle savedInstanceState) {        super.onViewCreated(view, savedInstanceState);        if (savedInstanceState == null){            presenter.fetchMovies(0);        } else {            List<MovieModel> movieModels = savedInstanceState.getParcelableArrayList("movies");            presenter.fetchMoviesFromState(movieModels);        }        refreshLayout.setOnRefreshListener(this);    }    @Override    public void onRefresh() {        refreshLayout.setRefreshing(false);        presenter.fetchMovies(0);    }    public interface OnFragmentInteractionListener {        // TODO: Update argument type and name        void onFragmentInteraction(Uri uri);    }    @Override    public void onSaveInstanceState(@NonNull Bundle outState) {        super.onSaveInstanceState(outState);    }}